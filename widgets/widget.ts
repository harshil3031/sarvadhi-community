/**
 * Widget Entry Point for Expo Widgets
 * This file configures the notification widget that appears on the home screen
 */

import { registerRootComponent } from 'expo';
import NotificationWidget from './NotificationWidget';

registerRootComponent(NotificationWidget);
